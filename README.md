# Ordini-Lampo API (Railway)

API Express per Ordini-Lampo.

## Avvio locale
1) Crea `.env` (NON committare):
   - DATABASE_URL=postgresql://...
   - NODE_ENV=development
2) Avvia:
   - npm install
   - npm run dev

## Health
- GET /api/v1/health
